# Webhook gateway

This is the shared initial skeleton for case012. It defines the public storage boundary, an HTTP router entry point, and a compatibility test. The implementation is intentionally incomplete and will be built by the model from the frozen prompt.
